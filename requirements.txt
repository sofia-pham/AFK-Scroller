# run: pip install -r .\requirements.txt to install these packages
# if the above command does not work, run: py -3.11 -m pip install -r .\requirements.txt 
opencv-python==4.5.5.64
mediapipe==0.10.11
pynput==1.7.6
numpy==1.21.3
# # Install if on Mac Silicon
# mediapipe-silicon==0.9.2.1
